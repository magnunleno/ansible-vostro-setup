- name: Set Git Credential Name
  git_config:
    name: user.name
    scope: global
    value: "{{ fullname }}"

- name: Set Git Credential Email
  git_config:
    name: user.email
    scope: global
    value: "{{ email }}"
